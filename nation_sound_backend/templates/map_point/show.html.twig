{% extends 'base.html.twig' %}

{% block title %}MapPoint{% endblock %}

{% block body %}
    <h1>MapPoint</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ map_point.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ map_point.type }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ map_point.name }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ map_point.position }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ map_point.description }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ map_point.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_map_point_index') }}">back to list</a>

    <a href="{{ path('app_map_point_edit', {'id': map_point.id}) }}">edit</a>

    {{ include('map_point/_delete_form.html.twig') }}
{% endblock %}
